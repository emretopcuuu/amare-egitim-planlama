# emretopcu.com - Netlify yapılandırması
# Netlify'da yeni site oluştururken "Base directory" = emretopcu-com seçilirse
# bu dosya otomatik okunur.

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
